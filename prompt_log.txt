## Student Name:Ali Ashraf
## Student ID: 218990184
llm prompts and response :{
Prompt 1:

We are trying to build an event enrollment system. The system is for registering users for a single event with a finite capacity. Users can register for the event until the capacity is reached, at which point they are placed on a waitlist. If a user cancels their registration, the next user on the waitlist is added to the registered list. All operations within the system must be deterministic.

Response summary:
The LLM suggested maintaining two ordered collections (registered list and FIFO waitlist). It emphasized deterministic processing, preventing duplicate registrations, and allowing users to check their status in the system.

Decision: Accept

Prompt 2:

As developers we are trying to build the system so it can work with the determined users of the system. Here are the personas we are working with:

Jane is a busy professional who prefers automation. The system should automatically resolve conflicts and provide deterministic behavior so that Jane does not have to interact with the system.

Mo is an edge user who has accessibility needs. Mo is very careful about the outputs from the system and gets stressed if the system does not provide an explanation for its actions and behaviors. The system should exhibit deterministic behavior and handle errors appropriately.

Response summary:
The LLM identified that the system must support deterministic behavior, automatic conflict resolution, and explicit error handling. It highlighted the importance of providing clear system feedback when invalid operations occur to support Mo’s accessibility needs.

Decision: Accept

Prompt 3:

Based on the system we are trying to build and the requirements of our personas, here are the system constraints and persona-driven constraints we will be using. The constraints include rules about event capacity, FIFO waitlist ordering, preventing duplicate registrations, deterministic system behavior, explicit error handling, and automatic waitlist promotion. Considering these system and persona constraints, are there any additional design considerations or improvements that should be taken into account when implementing the event registration system?

Response summary:
The LLM recommended using structured data structures such as ordered lists or queues to maintain deterministic ordering. It also suggested validating system invariants after every operation and improving error handling explanations for failed actions.

Decision: Modify
(The suggestions about state validation and sequential operation processing were incorporated into the specification.)

Prompt 4:

Here are the acceptance criteria we are planning to use for the system. They describe how the system should behave when users register, cancel registrations, attempt duplicate registrations, or interact with the waitlist. Considering the system constraints and persona requirements described earlier, do these acceptance criteria sufficiently cover the system behavior, or are there any gaps or improvements that should be considered?

Response summary:
The LLM confirmed that the acceptance criteria cover the core system behavior but suggested improvements such as validating system invariants, clarifying deterministic ordering of operations, and ensuring accurate status responses for users.

Decision: Modify
(Clarifications were incorporated into edge cases and revision notes.)

Prompt 5:

Review the system specification, constraints, and acceptance criteria for the event registration system and identify possible issues such as missing constraints, ambiguous requirements, or unhandled edge cases.

Response summary:
The LLM identified several potential issues including missing constraints related to capacity invariants, ambiguity around repeated cancellations, deterministic ordering during rapid registrations, and missing edge cases such as querying unknown users or updating waitlist positions after cancellations.

Decision: Accept


Prompt 6:
We previously implemented an event registration system for Lab 8 that supports registering users, maintaining a FIFO waitlist, promoting waitlisted users after cancellations, and preventing duplicate registrations. After incorporating the personas Jane and Mo, new requirements were added such as deterministic sequential processing, explicit error explanations for invalid operations, and clearer handling of edge cases such as zero event capacity and cancelling users not in the system. Based on these updated requirements, how should the existing implementation be modified to better support the persona-driven constraints?

Response summary:
The LLM suggested improving the existing implementation by adding clearer error handling messages when exceptions occur, ensuring operations remain deterministic by using ordered lists for registered users and the waitlist, and explicitly handling edge cases such as zero-capacity events and invalid cancellation attempts. It also recommended validating system invariants after each operation to maintain consistency and ensure that registered users never exceed capacity.

Good news: your Lab 8 code already satisfies most of the new persona requirements.
You only need small improvements to support the new constraints from Lab 9, mainly:

Clear error explanations for Mo (C3)

Deterministic sequential operations (Revision Note 1)

Explicit edge-case handling (Revision Note 4)

Clearer failure handling (C5)

So the code should be slightly extended, not rewritten.

Below is the updated version of your code with only the necessary changes.

code was provided: 
## Student Name: Ali Ashraf
## Student ID: 218990184

from dataclasses import dataclass
from typing import List, Optional


class DuplicateRequest(Exception):
    """Raised when a user attempts to register more than once."""
    pass


class NotFound(Exception):
    """Raised when a user cancellation is attempted for a non-existent user."""
    pass


@dataclass(frozen=True)
class UserStatus:
    state: str
    position: Optional[int] = None


class EventRegistration:

    def __init__(self, capacity: int) -> None:
        # Constraint C6: capacity must be non-negative
        if capacity < 0:
            raise ValueError("Event capacity must be a non-negative value.")

        self.capacity = capacity
        self.registered: List[str] = []
        self.waitlist: List[str] = []

    def register(self, user_id: str) -> UserStatus:
        # C2: prevent duplicate registrations
        if user_id in self.registered or user_id in self.waitlist:
            raise DuplicateRequest(
                f"User '{user_id}' is already registered or waitlisted."
            )

        # AC6: capacity = 0 → everyone waitlisted
        if self.capacity == 0:
            self.waitlist.append(user_id)
            return UserStatus("waitlisted", len(self.waitlist))

        # AC1: register if capacity available
        if len(self.registered) < self.capacity:
            self.registered.append(user_id)
            return UserStatus("registered")

        # AC2: otherwise waitlist (FIFO)
        self.waitlist.append(user_id)
        return UserStatus("waitlisted", len(self.waitlist))

    def cancel(self, user_id: str) -> None:

        # Registered user cancellation
        if user_id in self.registered:
            self.registered.remove(user_id)

            # AC3: promote earliest waitlisted user
            if self.waitlist and self.capacity > 0:
                promoted_user = self.waitlist.pop(0)
                self.registered.append(promoted_user)

            return

        # Waitlisted user cancellation
        if user_id in self.waitlist:
            self.waitlist.remove(user_id)
            return

        # C3 + AC5: explicit failure explanation
        raise NotFound(f"User '{user_id}' does not exist in the system.")

    def status(self, user_id: str) -> UserStatus:

        if user_id in self.registered:
            return UserStatus("registered")

        if user_id in self.waitlist:
            position = self.waitlist.index(user_id) + 1
            return UserStatus("waitlisted", position)

        # explicit status
        return UserStatus("none")

    def snapshot(self) -> dict:
        return {
            "registered": list(self.registered),
            "waitlist": list(self.waitlist),
        }


Prompt 7:
We previously implemented tests for the event registration system that validate the basic functionality such as registration, waitlist placement, promotion after cancellation, and duplicate registration prevention. After incorporating the personas Jane and Mo, additional requirements were introduced such as deterministic system behavior, explicit error handling, and clearer handling of edge cases. What additional test cases should be added to verify that the system satisfies these persona-driven requirements?

Response summary:
The LLM suggested adding additional tests that validate deterministic behavior by ensuring identical sequences of operations produce identical system states. It also recommended adding tests to verify explicit error handling when invalid actions occur, such as cancelling a user not present in the system. Additional edge case tests were suggested for scenarios such as querying the status of an unknown user and multiple users attempting to register when only one spot remains. These tests help verify that the system behaves consistently and handles edge conditions appropriately.

code: 
Great — for Lab 9 Task 8, the new tests should reflect the persona-driven requirements (Jane → deterministic behavior, Mo → explicit error handling + clear outputs) and include at least 2 edge cases.

Below are 5 clean tests you can append to your file.
I also included comments linking them to C#, AC#, and EC# so they match your traceability table.

New Test 1 — Deterministic Behavior (Jane)
# C2: deterministic system behavior
# AC1, AC2
def test_deterministic_behavior_same_sequence():
    er1 = EventRegistration(capacity=2)
    er2 = EventRegistration(capacity=2)

    sequence = ["u1", "u2", "u3", "u4"]

    for u in sequence:
        er1.register(u)

    for u in sequence:
        er2.register(u)

    # same operations must produce identical system state
    assert er1.snapshot() == er2.snapshot()
New Test 2 — Explicit Error Handling (Mo)
# C3: system must explain invalid actions
# AC5
def test_cancel_unknown_user_raises_notfound():
    er = EventRegistration(capacity=2)

    with pytest.raises(NotFound):
        er.cancel("unknown_user")
New Test 3 — Sequential Operations Consistency (Jane)
# C1: consistent behavior during repeated operations
# AC3
def test_multiple_cancellations_sequential_promotion():
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.register("u2")
    er.register("u3")

    er.cancel("u1")  # promotes u2
    er.cancel("u2")  # promotes u3

    assert er.status("u3") == UserStatus("registered")
New Test 4 — Edge Case: Unknown User Status
# EC: querying user not in system
# C3
def test_status_for_unknown_user():
    er = EventRegistration(capacity=2)

    status = er.status("ghost")

    assert status == UserStatus("none")
New Test 5 — Edge Case: Rapid Registrations with One Slot
# EC6: multiple users registering when one slot remains
# C1, C5
def test_multiple_users_one_remaining_slot():
    er = EventRegistration(capacity=1)

    s1 = er.register("u1")
    s2 = er.register("u2")
    s3 = er.register("u3")

    assert s1 == UserStatus("registered")
    assert s2 == UserStatus("waitlisted", 1)
    assert s3 == UserStatus("waitlisted", 2)

    snap = er.snapshot()
    assert snap["registered"] == ["u1"]


Decision: Accept
}
