## Student Name:Ali Ashraf
## Student ID: 218990184

Prompt #1: {help me with creating a pyton code which follows the given criterias , before coding jot down whether any ambiguities or missing clarifications exist criteria:nputs and Outputs Inputs: Describe each input parameter. What type and format do each take? What values are valid? inputs: we should have a event registration application , we need a fixed capacity fir how many spots are available for the event , user signup until capacity is reached for registration , each user should be able to do request for the registration like cancellation request check our waitlist status . command line should be used for registration such as signup, cancel registration or chekc your status of waitlist or registration capacity- type: integer- valid formats: Non-negative whole number userid- type: String/Integer, could be either of both however string probably works better as then we can use abc and 123 - format: any unique string not already used , cannot have duplicate unserid strings command - type: String - valid format: command keywords like cancel or status request- type: user request- valid format: userid + command , ex User request to cancel or request status Question #4 Output: Describe what the function returns. 4 Response is requiredResponse is required Output: Describe what the function returns. outputs: event registrations with a waitlist our outputs should only be the status of our registration , do we have a spot or do we not have a spot or are we on a waitlist for a spot event_state: the output returned should give the state of the current registration for the event is it full or is there still space , is there going to be space for a waitlist or not , etc user_event_state: the output should return to the event state for the user, was the user able to signup or did the user cancel his spot for the event , is the user on the waitlist etc Question #5 Functional RequirementsList as many as functional requirements as possible from the specification description.&nbsp; 5 Response is requiredResponse is required Functional Requirements List as many as functional requirements as possible from the specification description. The system shall accept an event capacity when the registration system is started. The system shall allow users to register for the event. The system shall add a user to the registered list if the number of registered users is below the capacity. The system shall add a user to a waitlist if the capacity has been reached. The system shall add users to waitlist in FIFO order The system shall automatically add the earliest waitlisted user when a registered user cancels The system shall prevent duplicate user registrations. The system shall allow users to cancel their registration The system shall allow users to check current status of their registration The system shall ensure the number of registered users never exceeds the capacity The system shall ensure users may not appear more than once in event registration The system shall update the system state after every operation Question #6 ConstraintsList at least&nbsp;6 constraints&nbsp;and assumptions relevant to the problem, in a bullet point list.1.&nbsp;2.&nbsp;3.&nbsp;... 6 Response is requiredResponse is required Constraints List at least 6 constraints and assumptions relevant to the problem, in a bullet point list. 1. 2. 3. ... 1. the number of users registered for the event should not reach more than the available capacity 2. Each user should only appear once in the event registration 3. the waitlist must follow FIFO order 4. a user must either be in a registered user list or a waitlist, cannot be in both at the same time 5. if a registered user cancels there registration the earliest waitlist user must be moved to the registered registration list 6. event capacity must be a non negative value Question #7 InvariantsList at least&nbsp;3 invariants&nbsp;that must always hold&nbsp;in this case, in a bullet point list.1.&nbsp;2.3. 7 Response is requiredResponse is required Invariants List at least 3 invariants that must always hold in this case, in a bullet point list. 1. 2. 3. 1. the number of users registered for the event should not reach more than the available capacity 2. A user must not appear more than once in the system. 3. A user must exist in only one state at a time( such as registered or waitlist or none ) Question #8 Negative&nbsp;RequirementsList at least&nbsp;two negative requirements&nbsp;(situations that must&nbsp;not&nbsp;occur).&nbsp; 8 Response is requiredResponse is required Negative Requirements List at least two negative requirements (situations that must not occur). The system must not allow a user to be registered more than once. The system must not allow the number of registered users to exceed the event capacity Question #9 Tie‑Breaking RulesExplain how the system should choose between multiple feasible scenarios. If two scenarios satisfy the same constraints, which should be prioritized?&nbsp; 9 Response is requiredResponse is required Tie‑Breaking Rules Explain how the system should choose between multiple feasible scenarios. If two scenarios satisfy the same constraints, which should be prioritized? for scenarios related to event registration and place in waitlist following FIFO , the system should prioritize the user who selected joined first. in the waitlist the earliest joined user should be the person who moves up to the registered queue if multiple commands are selected , then the system must also follow a FIFO, they must be processed in the order they were received Question #10 Exceptions and Day‑Specific RulesDocument any special rules or exceptions. Include any other specific constraints you can think of. 10 Response is requiredResponse is required Exceptions and Day‑Specific Rules Document any special rules or exceptions. Include any other specific constraints you can think of. If the event capacity is set to 0( not determine yet), all registration users should be added to the waitlist if a user on the waitlist cancels his registration , then his place in the queue should be filled in by the next waitlisted user who registered directly after them and the canceled user must be removed from FIFO, if they re enter the waitlist they should be put at the end of the waitlist based on FIFO Question #11 Acceptance CriteriaMinimum 6 requiredEach AC must link to at least one Constraint ID.AC1:Given …When …Then …Linked Constraint ID(s): C#Guidelines:AC must be verifiable.Avoid narrative justification 11 Response is requiredResponse is required Acceptance Criteria Minimum 6 required Each AC must link to at least one Constraint ID. AC1: Given … When … Then … Linked Constraint ID(s): C# Guidelines: AC must be verifiable. Avoid narrative justification AC1: Given an event with capacity N When a user registers and the number of registered users is less than N Then the user is added to the registered users list Linked Constraint ID: C1 AC2: Given an event where the registered users list is full When a new user registers Then the user is added to the waitlist Linked Constraint ID: C1, C3 AC3: Given a registered user cancels their registration When there are users on the waitlist Then the earliest waitlisted user is moved to the registered list Linked Constraint ID(s): C3, C5 AC4: Given a user already exists in the system When the user attempts to register again Then the system rejects the request Linked Constraint ID: C2 AC5: Given a user checks their status When the user exists in the system Then the system returns whether the user is registered or waitlisted with their waitlist position Linked Constraint ID: C4 AC6: Given an event with capacity equal to zero When users attempt to register Then all users are placed on the waitlist and none are registered Linked Constraint ID: C1, C6 Question #12 Edge CasesList edge or boundary conditions the system must handle.For each edge case, write like:EC1:Description:Linked Constraint ID(s): C#Covered by Acceptance Criteria ID(s): AC#Examples:Zero available slotsCapacity = 0Duplicate cancellationAdjacent intervalsMinimal gap smaller than duration 12 Response is requiredResponse is required Edge Cases List edge or boundary conditions the system must handle. For each edge case, write like: EC1: Description: Linked Constraint ID(s): C# Covered by Acceptance Criteria ID(s): AC# Examples: Zero available slots Capacity = 0 Duplicate cancellation Adjacent intervals Minimal gap smaller than duration EC1: Description: capacity is set to 0 and users attempt to register. Linked Constraint ID: C1, C6 Covered by Acceptance Criteria ID: AC6 EC2: Description: A user attempts to register more than once. Linked Constraint ID(s): C2 Covered by Acceptance Criteria ID: AC4 EC3: Description: A registered user cancels and there are users on the waitlist. Linked Constraint ID: C3, C5 Covered by Acceptance Criteria ID: AC3 EC4: Description: A waitlisted user cancels their request before being moved to registered user list . Linked Constraint ID: C4 Covered by Acceptance Criteria ID: AC5 Question #13 Traceability TableComplete the table below.Constraint ID &amp;Derived From Where &amp;&nbsp;Linked AC IDs &amp;&nbsp;Linked Test Name(s)C1WhereAC1TestNameInstructions:Every Constraint must link to at least one AC.Every Constraint must eventually be validated by at least one test.Tests must include a comment indicating which Constraint they validate. 13 Response is requiredResponse is required Traceability Table Complete the table below. Constraint ID & Derived From Where & Linked AC IDs & Linked Test Name(s) C1 Where AC1 TestName Instructions: Every Constraint must link to at least one AC. Every Constraint must eventually be validated by at least one test. Tests must include a comment indicating which Constraint they validate. Constraint ID Derived From Where Linked AC IDs Linked Test Name(s) C1 Event capacity requirement system specification AC1, AC2 capacity_limit_test C2 Duplicate registration rule AC4 duplicate_registration_test C3 FIFO waitlist ordering requirement AC2, AC3 waitlist_order_test C4 User must exist in only one state AC5 user_status_test C5 Waitlist promotion after cancellation AC3 waitlist_promotion_test C6 Event capacity must be non-negative AC6 zero_capacity_test code:here is the code we have : ## Student Name: ## Student ID: """ Task B: Event Registration with Waitlist (Stub) In this lab, you will design and implement an Event Registration with Waitlist system using an LLM assistant as your primary programming collaborator. You are asked to implement a Python module that manages registration for a single event with a fixed capacity. The system must: • Accept a fixed capacity. • Register users until capacity is reached. • Place additional users into a FIFO waitlist. • Automatically promote the earliest waitlisted user when a registered user cancels. • Prevent duplicate registrations. • Allow users to query their current status. The system must ensure that: • The number of registered users never exceeds capacity. • Waitlist ordering preserves FIFO behavior. • Promotions occur deterministically under identical operation sequences. The module must preserve the following invariants: • A user may not appear more than once in the system. • A user may not simultaneously exist in multiple states. • The system state must remain consistent after every operation. The system must correctly handle non-trivial scenarios such as: • Multiple cancellations in sequence. • Users attempting to re-register after canceling. • Waitlisted users canceling before promotion. • Capacity equal to zero. • Simultaneous or rapid consecutive operations. • Queries during state transitions. The output consists of the updated registration state and ordered lists of registered and waitlisted users after each operation. """ from dataclasses import dataclass from typing import List, Optional class DuplicateRequest(Exception): """Raised if a user tries to register but is already registered or waitlisted.""" pass class NotFound(Exception): """Raised if a user cannot be found for cancellation (if required by handout).""" pass @dataclass(frozen=True) class UserStatus: """ state: - "registered" - "waitlisted" - "none" position: 1-based waitlist position if waitlisted; otherwise None """ state: str position: Optional[int] = None class EventRegistration: """ Students must implement this class per the lab handout. Deterministic ordering is required (e.g., FIFO waitlist, predictable registration order). """ def __init__(self, capacity: int) -> None: """ Args: capacity: maximum number of registered users (>= 0) """ # TODO: Initialize internal data structures raise NotImplementedError("EventRegistration.__init__ not implemented yet") def register(self, user_id: str) -> UserStatus: """ Register a user: - if capacity available -> registered - else -> waitlisted (FIFO) Raises: DuplicateRequest if user already exists (registered or waitlisted) """ # TODO: Implement per lab handout raise NotImplementedError("register not implemented yet") def cancel(self, user_id: str) -> None: """ Cancel a user: - if registered -> remove and promote earliest waitlisted user (if any) - if waitlisted -> remove from waitlist - behavior when user not found depends on handout (raise NotFound or ignore) Raises: NotFound (if required by handout) """ # TODO: Implement per lab handout raise NotImplementedError("cancel not implemented yet") def status(self, user_id: str) -> UserStatus: """ Return status of a user: - registered - waitlisted with position (1-based) - none """ # TODO: Implement per lab handout raise NotImplementedError("status not implemented yet") def snapshot(self) -> dict: """ (Optional helper for debugging/tests) Return a deterministic snapshot of internal state. """ # TODO: Implement if required/allowed raise NotImplementedError("snapshot not implemented yet")}


Response #1: {Before writing the Python code, the lab requires asking whether ambiguities or missing clarifications exist. The manual explicitly states that you must ask this first before implementation. 
Below is a structured analysis of possible ambiguities and missing clarifications in your specification.

Ambiguities / Missing Clarifications in the Requirements
1. Command Line Interface Format

You stated commands like:

signup

cancel

status

But the exact command syntax is not defined.

Possible interpretations:

signup <userid>
cancel <userid>
status <userid>

OR

<userid> signup
<userid> cancel
<userid> status

🔹 Clarification needed:
What exact format should the CLI use?

Example question:

Should commands follow command user_id or user_id command format?

2. Behavior When Cancelling a Non-existent User

Your code stub includes an exception:

class NotFound(Exception)

But the specification says:

behavior when user not found depends on handout (raise NotFound or ignore)

Your requirements do not explicitly specify what should happen.

Possible behaviors:

Option A

raise NotFound

Option B

ignore silently

🔹 Clarification needed:
Should the system raise NotFound if a user that does not exist attempts cancellation?

3. Behavior When Checking Status of Unknown User

For status(user_id) you said output should be:

registered

waitlisted

none

But the stub says:

return UserStatus(state="none")

🔹 Clarification needed:

Should status of a non-existing user return:

UserStatus(state="none")

or raise an error?

4. Maximum Waitlist Size

You defined capacity for registered users, but not for the waitlist.

Possible interpretations:

Option A
Unlimited waitlist

Option B
Waitlist has a maximum size

Your requirements imply unlimited but do not explicitly state it.

🔹 Clarification needed:

Is the waitlist unbounded (unlimited size)?

5. Order of Registered Users

You defined FIFO for waitlist, but not for registered users.

Example:

Users register:

A
B
C

Registered list becomes:

[A, B, C]

If A cancels, and D is promoted, do we append D to end?

Result possibilities:

Option A

[B, C, D]

Option B

[D, B, C]

Most systems use append to end.

🔹 Clarification needed:

When a waitlisted user is promoted, should they be appended to the end of the registered list?

6. Behavior if Capacity is Negative

Constraint says:

event capacity must be non negative

But the constructor does not specify what to do if:

capacity = -1

Possible behavior:

Option A

raise ValueError

Option B

convert to 0

🔹 Clarification needed:

Should the constructor raise ValueError if capacity < 0?

7. What Snapshot() Should Return

The stub includes:

def snapshot(self) -> dict

But the requirements do not define its structure.

Possible format:

{
 "capacity": 5,
 "registered": ["A","B","C"],
 "waitlist": ["D","E"]
}

🔹 Clarification needed:

What fields must snapshot() include?

8. Duplicate Detection Rules

You stated:

cannot have duplicate userid strings

But not whether case sensitivity matters.

Example:

Alice
alice

Possible interpretations:

Option A

different users

Option B

same user

🔹 Clarification needed:

Are user IDs case-sensitive?

9. Return Value of Cancel Operation

Your function:

cancel(user_id) -> None

But your outputs specification says system should show event state.

Possible outputs:

Option A

None

Option B

UserStatus

Option C

snapshot()

🔹 Clarification needed:

Should cancel() return nothing, or return the updated user status/system state?

10. Concurrency / Simultaneous Requests

Your specification mentions:

simultaneous or rapid consecutive operations

But no concurrency model is defined.

Since this is a single Python module, it likely means:

operations executed sequentially

🔹 Clarification needed:

Should the system assume sequential execution only?

Recommended Assumptions (If Not Clarified)

If the professor did not specify, typical assumptions are:

Issue	Assumption
Command format	command user_id
Cancel unknown user	raise NotFound
Status unknown user	return "none"
Waitlist size	unlimited
Promotion order	append to end of registered list
Negative capacity	raise ValueError
Snapshot format	{capacity, registered, waitlist}
User ID case	case-sensitive
Cancel return	None
Execution	sequential


response: 
1. Fixed Capacity
self.capacity = capacity

Registration is only allowed while:

len(self.registered) < self.capacity
2. FIFO Waitlist
self.waitlist.append(user_id)

Promotion:

promoted = self.waitlist.pop(0)

This ensures First-In-First-Out behavior.

3. Prevent Duplicate Users
if user_id in self.registered or user_id in self.waitlist:
    raise DuplicateRequest

Ensures:

user appears only once

user cannot be registered AND waitlisted

4. Automatic Promotion After Cancellation
if self.waitlist:
    promoted = self.waitlist.pop(0)
    self.registered.append(promoted)

Earliest waitlisted user is moved.

5. Waitlist Cancel

If user cancels while waitlisted:

self.waitlist.remove(user_id)

Queue automatically shifts.

6. Capacity = 0 Edge Case

If capacity is zero:

len(self.registered) < self.capacity

is always false → everyone goes to waitlist

7. Deterministic Behavior

Lists preserve order:

registered = []
waitlist = []

Operations always produce the same result for the same sequence.

Example Usage
event = EventRegistration(2)

print(event.register("A"))
print(event.register("B"))
print(event.register("C"))

print(event.snapshot())

event.cancel("A")

print(event.snapshot())

print(event.status("C"))

Output example:

UserStatus(state='registered', position=None)
UserStatus(state='registered', position=None)
UserStatus(state='waitlisted', position=1)

{'capacity': 2, 'registered': ['A', 'B'], 'waitlist': ['C']}

{'capacity': 2, 'registered': ['B', 'C'], 'waitlist': []}

UserStatus(state='registered', position=None)

code: from dataclasses import dataclass
from typing import List, Optional


class DuplicateRequest(Exception):
    """Raised if a user tries to register but is already registered or waitlisted."""
    pass


class NotFound(Exception):
    """Raised if a user cannot be found for cancellation."""
    pass


@dataclass(frozen=True)
class UserStatus:
    state: str
    position: Optional[int] = None


class EventRegistration:

    def __init__(self, capacity: int) -> None:
        """
        capacity: maximum number of registered users (>=0)
        """
        if capacity < 0:
            raise ValueError("Capacity must be non-negative")

        self.capacity = capacity
        self.registered: List[str] = []
        self.waitlist: List[str] = []

    def register(self, user_id: str) -> UserStatus:
        """
        Register a user.
        """
        # Prevent duplicate users
        if user_id in self.registered or user_id in self.waitlist:
            raise DuplicateRequest("User already registered or waitlisted")

        # If space available -> register
        if len(self.registered) < self.capacity:
            self.registered.append(user_id)
            return UserStatus("registered")

        # Otherwise -> add to FIFO waitlist
        self.waitlist.append(user_id)
        position = len(self.waitlist)
        return UserStatus("waitlisted", position)

    def cancel(self, user_id: str) -> None:
        """
        Cancel registration or waitlist.
        """

        # Case 1: user is registered
        if user_id in self.registered:
            self.registered.remove(user_id)

            # Promote earliest waitlisted user
            if self.waitlist:
                promoted = self.waitlist.pop(0)
                self.registered.append(promoted)

            return

        # Case 2: user is waitlisted
        if user_id in self.waitlist:
            self.waitlist.remove(user_id)
            return

        # Case 3: user not found
        raise NotFound("User not found in system")

    def status(self, user_id: str) -> UserStatus:
        """
        Return the user's current status.
        """

        if user_id in self.registered:
            return UserStatus("registered")

        if user_id in self.waitlist:
            position = self.waitlist.index(user_id) + 1
            return UserStatus("waitlisted", position)

        return UserStatus("none")

    def snapshot(self) -> dict:
        """
        Deterministic snapshot of system state.
        Useful for debugging or testing.
        """
        return {
            "capacity": self.capacity,
            "registered": list(self.registered),
            "waitlist": list(self.waitlist)
        }
}

prompt 2:{ here is my modified version of the code , is there anything i am missing or left incorrct
## Student Name: Ali Ashraf
## Student ID: 218990184

"""
Task B: Event Registration with Waitlist (Stub)
In this lab, you will design and implement an Event Registration with Waitlist system using an LLM assistant as your primary programming collaborator. 
You are asked to implement a Python module that manages registration for a single event with a fixed capacity. 
The system must:
•	Accept a fixed capacity.
•	Register users until capacity is reached.
•	Place additional users into a FIFO waitlist.
•	Automatically promote the earliest waitlisted user when a registered user cancels.
•	Prevent duplicate registrations.
•	Allow users to query their current status.

The system must ensure that:
•	The number of registered users never exceeds capacity.
•	Waitlist ordering preserves FIFO behavior.
•	Promotions occur deterministically under identical operation sequences.

The module must preserve the following invariants:
•	A user may not appear more than once in the system.
•	A user may not simultaneously exist in multiple states.
•	The system state must remain consistent after every operation.

The system must correctly handle non-trivial scenarios such as:
•	Multiple cancellations in sequence.
•	Users attempting to re-register after canceling.
•	Waitlisted users canceling before promotion.
•	Capacity equal to zero.
•	Simultaneous or rapid consecutive operations.
•	Queries during state transitions.

The output consists of the updated registration state and ordered lists of registered and waitlisted users after each operation.
"""

from dataclasses import dataclass
from typing import List, Optional


class DuplicateRequest(Exception):
    pass


class NotFound(Exception):
    pass


@dataclass(frozen=True)
class UserStatus:
    state: str
    position: Optional[int] = None


class EventRegistration:

    def __init__(self, capacity: int) -> None:
        # Constraint C6: capacity must be non-negative
        if capacity < 0:
            raise ValueError("capacity must be non-negative")

        self.capacity = capacity
        self.registered: List[str] = []
        self.waitlist: List[str] = []

    def register(self, user_id: str) -> UserStatus:
        # Constraint C2: prevent duplicate users
        if user_id in self.registered or user_id in self.waitlist:
            raise DuplicateRequest()

        # AC1: register if capacity available
        if len(self.registered) < self.capacity:
            self.registered.append(user_id)
            return UserStatus("registered")

        # AC2: otherwise waitlist (FIFO)
        self.waitlist.append(user_id)
        return UserStatus("waitlisted", len(self.waitlist))

    def cancel(self, user_id: str) -> None:

        # If registered → remove and promote waitlisted user
        if user_id in self.registered:
            self.registered.remove(user_id)

            # AC3: promote earliest waitlisted user
            if self.waitlist and self.capacity > 0:
                promoted_user = self.waitlist.pop(0)
                self.registered.append(promoted_user)

            return

        # If waitlisted → remove from waitlist
        if user_id in self.waitlist:
            self.waitlist.remove(user_id)
            return

        # Edge case: cancel unknown user
        raise NotFound()

    def status(self, user_id: str) -> UserStatus:

        if user_id in self.registered:
            return UserStatus("registered")

        if user_id in self.waitlist:
            position = self.waitlist.index(user_id) + 1
            return UserStatus("waitlisted", position)

        return UserStatus("none")

    def snapshot(self) -> dict:
        return {
            "registered": list(self.registered),
            "waitlist": list(self.waitlist),
        }
}
repsonse2 {:Your code is already very close to correct, but I cleaned it slightly so it fully satisfies the requirements and keeps the state deterministic and consistent. Below is the final clean version :
## Student Name: Ali Ashraf
## Student ID: 218990184

"""
Task B: Event Registration with Waitlist (Stub)
In this lab, you will design and implement an Event Registration with Waitlist system using an LLM assistant as your primary programming collaborator. 
You are asked to implement a Python module that manages registration for a single event with a fixed capacity. 
The system must:
• Accept a fixed capacity.
• Register users until capacity is reached.
• Place additional users into a FIFO waitlist.
• Automatically promote the earliest waitlisted user when a registered user cancels.
• Prevent duplicate registrations.
• Allow users to query their current status.

The system must ensure that:
• The number of registered users never exceeds capacity.
• Waitlist ordering preserves FIFO behavior.
• Promotions occur deterministically under identical operation sequences.

The module must preserve the following invariants:
• A user may not appear more than once in the system.
• A user may not simultaneously exist in multiple states.
• The system state must remain consistent after every operation.

The system must correctly handle non-trivial scenarios such as:
• Multiple cancellations in sequence.
• Users attempting to re-register after canceling.
• Waitlisted users canceling before promotion.
• Capacity equal to zero.
• Simultaneous or rapid consecutive operations.
• Queries during state transitions.

The output consists of the updated registration state and ordered lists of registered and waitlisted users after each operation.
"""

from dataclasses import dataclass
from typing import List, Optional


class DuplicateRequest(Exception):
    pass


class NotFound(Exception):
    pass


@dataclass(frozen=True)
class UserStatus:
    state: str
    position: Optional[int] = None


class EventRegistration:

    def __init__(self, capacity: int) -> None:
        # Constraint C6: capacity must be non-negative
        if capacity < 0:
            raise ValueError("capacity must be non-negative")

        self.capacity = capacity
        self.registered: List[str] = []
        self.waitlist: List[str] = []

    def register(self, user_id: str) -> UserStatus:
        # Constraint C2: prevent duplicate users
        if user_id in self.registered or user_id in self.waitlist:
            raise DuplicateRequest()

        # AC1: register if capacity available
        if len(self.registered) < self.capacity:
            self.registered.append(user_id)
            return UserStatus("registered")

        # AC2: otherwise add to FIFO waitlist
        self.waitlist.append(user_id)
        return UserStatus("waitlisted", len(self.waitlist))

    def cancel(self, user_id: str) -> None:

        # If registered → remove and promote earliest waitlisted user
        if user_id in self.registered:
            self.registered.remove(user_id)

            # AC3: promote earliest waitlisted user
            if self.waitlist and len(self.registered) < self.capacity:
                promoted_user = self.waitlist.pop(0)
                self.registered.append(promoted_user)

            return

        # If waitlisted → remove from waitlist
        if user_id in self.waitlist:
            self.waitlist.remove(user_id)
            return

        # Edge case: cancel unknown user
        raise NotFound()

    def status(self, user_id: str) -> UserStatus:

        if user_id in self.registered:
            return UserStatus("registered")

        if user_id in self.waitlist:
            position = self.waitlist.index(user_id) + 1
            return UserStatus("waitlisted", position)

        return UserStatus("none")

    def snapshot(self) -> dict:
        return {
            "capacity": self.capacity,
            "registered": list(self.registered),
            "waitlist": list(self.waitlist),
        }

}
prompt 2: {how can i implement my edgetesting for this : 
here is py.test: import pytest from solution import EventRegistration, UserStatus, DuplicateRequest, NotFound def test_register_until_capacity_then_waitlist_fifo_positions(): er = EventRegistration(capacity=2) s1 = er.register("u1") s2 = er.register("u2") s3 = er.register("u3") s4 = er.register("u4") assert s1 == UserStatus("registered") assert s2 == UserStatus("registered") assert s3 == UserStatus("waitlisted", 1) assert s4 == UserStatus("waitlisted", 2) snap = er.snapshot() assert snap["registered"] == ["u1", "u2"] assert snap["waitlist"] == ["u3", "u4"] def test_cancel_registered_promotes_earliest_waitlisted_fifo(): er = EventRegistration(capacity=1) er.register("u1") er.register("u2") # waitlist er.register("u3") # waitlist er.cancel("u1") # should promote u2 assert er.status("u1") == UserStatus("none") assert er.status("u2") == UserStatus("registered") assert er.status("u3") == UserStatus("waitlisted", 1) snap = er.snapshot() assert snap["registered"] == ["u2"] assert snap["waitlist"] == ["u3"] def test_duplicate_register_raises_for_registered_and_waitlisted(): er = EventRegistration(capacity=1) er.register("u1") with pytest.raises(DuplicateRequest): er.register("u1") er.register("u2") # waitlisted with pytest.raises(DuplicateRequest): er.register("u2") def test_waitlisted_cancel_removes_and_updates_positions(): er = EventRegistration(capacity=1) er.register("u1") er.register("u2") # waitlist pos1 er.register("u3") # waitlist pos2 er.cancel("u2") # remove from waitlist assert er.status("u2") == UserStatus("none") assert er.status("u3") == UserStatus("waitlisted", 1) snap = er.snapshot() assert snap["registered"] == ["u1"] assert snap["waitlist"] == ["u3"] def test_capacity_zero_all_waitlisted_and_promotion_never_happens(): er = EventRegistration(capacity=0) assert er.register("u1") == UserStatus("waitlisted", 1) assert er.register("u2") == UserStatus("waitlisted", 2) # No one can ever be registered when capacity=0 assert er.status("u1") == UserStatus("waitlisted", 1) assert er.status("u2") == UserStatus("waitlisted", 2) assert er.snapshot()["registered"] == [] # Cancel unknown should raise NotFound with pytest.raises(NotFound): er.cancel("missing") ################################################################################# # Add your own additional tests here to cover more cases and edge cases as needed. #################################################################################

response: 
Your pytest file already covers the core functionality well. To fully satisfy the lab requirement “Add your own additional tests here to cover more cases and edge cases”, you should add a few extra edge-case tests.

Below are clean additional tests you can paste under the section:
def test_status_of_unknown_user_returns_none():
    er = EventRegistration(capacity=2)

    assert er.status("unknown") == UserStatus("none")


def test_cancel_unknown_user_raises_notfound():
    er = EventRegistration(capacity=2)

    with pytest.raises(NotFound):
        er.cancel("ghost")


def test_multiple_cancellations_promote_multiple_waitlisted():
    er = EventRegistration(capacity=2)

    er.register("u1")
    er.register("u2")
    er.register("u3")  # waitlist
    er.register("u4")  # waitlist

    er.cancel("u1")  # promote u3
    er.cancel("u2")  # promote u4

    assert er.status("u3") == UserStatus("registered")
    assert er.status("u4") == UserStatus("registered")

    snap = er.snapshot()
    assert snap["registered"] == ["u3", "u4"]
    assert snap["waitlist"] == []


def test_user_can_reregister_after_cancel():
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.cancel("u1")

    status = er.register("u1")

    assert status == UserStatus("registered")


def test_waitlist_fifo_order_after_multiple_registrations():
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.register("u2")
    er.register("u3")
    er.register("u4")

    snap = er.snapshot()

    assert snap["waitlist"] == ["u2", "u3", "u4"]
    assert er.status("u2") == UserStatus("waitlisted", 1)
    assert er.status("u3") == UserStatus("waitlisted", 2)
    assert er.status("u4") == UserStatus("waitlisted", 3)}

prompt3: {here is my test based on what you had provided: 

here is my test:# EC1: capacity = 0 def test_edge_case_capacity_zero(): er = EventRegistration(capacity=0) assert er.register("u1") == UserStatus("waitlisted", 1) assert er.register("u2") == UserStatus("waitlisted", 2) assert er.snapshot()["registered"] == [] # EC2: duplicate registration def test_edge_case_duplicate_registration(): er = EventRegistration(capacity=2) er.register("u1") with pytest.raises(DuplicateRequest): er.register("u1") # EC3: registered user cancels with waitlist def test_edge_case_promotion_after_cancel(): er = EventRegistration(capacity=1) er.register("u1") er.register("u2") er.cancel("u1") assert er.status("u2") == UserStatus("registered") # EC4: waitlisted user cancels def test_edge_case_waitlist_cancel(): er = EventRegistration(capacity=1) er.register("u1") er.register("u2") er.cancel("u2") assert er.status("u2") == UserStatus("none")}

repsonse 3: {our tests are good and mostly correct, but there are two small improvements that will make them stronger and more aligned with your Edge Cases (EC1–EC4) and Acceptance Criteria.

I’ll show you a clean improved version of your tests with only small fixes.
# EC1: capacity = 0
def test_edge_case_capacity_zero():
    er = EventRegistration(capacity=0)

    assert er.register("u1") == UserStatus("waitlisted", 1)
    assert er.register("u2") == UserStatus("waitlisted", 2)

    assert er.snapshot()["registered"] == []
    assert er.snapshot()["waitlist"] == ["u1", "u2"]


# EC2: duplicate registration
def test_edge_case_duplicate_registration():
    er = EventRegistration(capacity=2)

    er.register("u1")

    with pytest.raises(DuplicateRequest):
        er.register("u1")


# EC3: registered user cancels with waitlist
def test_edge_case_promotion_after_cancel():
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.register("u2")

    er.cancel("u1")

    assert er.status("u1") == UserStatus("none")
    assert er.status("u2") == UserStatus("registered")

    snap = er.snapshot()
    assert snap["registered"] == ["u2"]
    assert snap["waitlist"] == []


# EC4: waitlisted user cancels
def test_edge_case_waitlist_cancel():
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.register("u2")

    er.cancel("u2")

    assert er.status("u2") == UserStatus("none")

    snap = er.snapshot()
    assert snap["registered"] == ["u1"]
    assert snap["waitlist"] == []

Why These Changes Help
1️⃣ Stronger Verification

Example:

snap = er.snapshot()
assert snap["registered"] == ["u2"]
assert snap["waitlist"] == []

This ensures the system state is correct, not just the user status.
One Small Thing to Confirm

Make sure your test file has this import at the top:

import pytest
from solution import EventRegistration, UserStatus, DuplicateRequest

(You don't need NotFound for these tests.)
}
prompt 4: {add it a 5th test which validates the edge case}

response 4: { Good idea — adding one more test strengthens your submission and covers an additional edge case.

A very useful one is re-registering after cancellation, which your specification mentions:

users attempting to re-register after canceling. 

LAB8_MANUAL_01_X

Add this at the bottom of tests.py.

Additional Test Case
def test_reregister_after_cancel():
    er = EventRegistration(capacity=1)

    # user registers
    er.register("u1")

    # user cancels
    er.cancel("u1")

    # user should now be able to register again
    status = er.register("u1")

    assert status == UserStatus("registered")

    snap = er.snapshot()
    assert snap["registered"] == ["u1"]
    assert snap["waitlist"] == []
What this test verifies

This checks that:

a user can cancel

the user is removed from the system

the user can register again later

This aligns with your Exceptions and Edge Cases section. Why this test is good

It verifies an important requirement from your spec:

user cancels

user leaves system

user can register again later

Many students forget this case.

Your final test coverage
Test	What it verifies
capacity registration	normal operation
FIFO waitlist	correct ordering
promotion after cancel	waitlist promotion
duplicate registration	constraint
capacity = 0	edge case
re-register after cancel	edge case}

